# Dependencies
node_modules/

# Compiled native helpers (built from .swift source via build/compile-mac-helpers.sh
# in CI, or locally before first dev run on macOS). Only the .swift sources are
# tracked — ignoring everything else covers new helpers and the per-arch .tmp
# intermediates automatically.
electron/helpers/*
!electron/helpers/*.swift

# Build outputs
out/
dist/
dist-electron/

# electron-vite cache
.vite/

# electron-builder artifacts
release/
*.exe
*.dmg
*.AppImage
*.deb
*.rpm
*.snap
*.nsis

# Logs
logs/
*.log
npm-debug.log*

# Runtime / OS
.DS_Store
Thumbs.db
*.swp
*.swo

# Environment & secrets
.env
.env.local
.env.*.local
electron/gdrive-credentials.ts

# Editor
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TypeScript cache
*.tsbuildinfo

# electron-store data (user data, not source)
# (stored in app.getPath('userData'), not in repo)

# Claude Code local user settings (machine-specific, not shared)
.claude/settings.local.json
